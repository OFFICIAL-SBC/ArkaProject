spring:
    application:
        name: ms_cart

    datasource:
      url: jdbc:postgresql://localhost:5432/arkaproject
      username: app_owner
      password: Geco1964Cata18
      driver-class-name: org.postgresql.Driver

    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true

    rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest

logging:
    level:
        org.hibernate.SQL: DEBUG
        org.hibernate.type.descriptor.sql.BasicBinder: TRACE